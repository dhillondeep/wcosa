type: app

project:
  name: app-pthreads
  version: 0.0.1
  keywords:
  - wio
  - app
  compile_options:
    wio_version: 0.5.2
    default_target: main

targets:
  main:
    src: src
    platform: native
    link_libraries:
    - -pthreads

      #libraries:
      #Threads:
      # global: true
      #required: true
